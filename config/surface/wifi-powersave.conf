# Surface Pro 2 — disable WiFi power saving globally via NetworkManager.
# This is the bus-agnostic, always-valid fix for the flaky Marvell radio and
# works regardless of whether the adapter is the USB or PCIe variant.
#   2 = disable power save
[connection]
wifi.powersave = 2
