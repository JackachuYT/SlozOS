# Surface Book 1 — disable WiFi power saving globally via NetworkManager.
# Prevents the Marvell 88W8897 (PCIe) from dropping the connection on idle.
#   2 = disable power save
[connection]
wifi.powersave = 2
